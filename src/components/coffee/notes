do i need providers for each foreign key such as "flavorId": 1,
            "typeOfCoffeeId": 1,
            "actorRatingId": 1,


coffee.sort((a,b) => new Date(b.date) - new Date(a.date)).map(coffee => {
                return <CoffeeCard key={coffee.id} coffee={coffee}/>
              })